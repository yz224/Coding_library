3_Use

'go to local repo'

cd ~/...

'look current situation'

git status

'add changed file'

git add --all : add all changed files

'commit changed file'

git commit -m 'message'

‘looking back what commits you’ve made’

git log —-pretty=oneline
(press q to quit)

llll
